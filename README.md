# Natter API (Node.js + Fastify)

A Node.js implementation of the Natter API from API Security in Action by Neil Madden.

This project is a lightweight REST API built with Fastify, designed to demonstrate modern API security concepts such as authentication, authorization, and secure token handling.

## Environment Variables

Create a .env file:

```
DATABASE_URL=your_database_url
```